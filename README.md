# 🖥️ free-rdp

Проект **free-rdp** предоставляет простой способ запуска удалённого рабочего стола (RDP) с поддержкой noVNC, QEMU/KVM и Docker.

## 🚀 Возможности

- Поддержка **noVNC** для веб-доступа к RDP
- Поддержка **QEMU** и **KVM**
- Автоматический запуск Docker Daemon на десктопах
- Образы на **Docker Hub**
- Поддержка **Debian Bookworm**
- Доступ к терминалу через веб
- Исправлены ошибки Forbidden IP при установке
- Устранены многочисленные баги

## 📦 Установка

### 1. Сборка образа

```bash
docker build -t free-rdp:latest -f docker/Dockerfile .
```

### 2. Настройка Docker и запуск

```bash
docker-compose -f compose/docker-compose.yml up -d
```
Docker автоматически загрузит образ и запустит необходимые сервисы.

## Поддержка RDP over UDP

Для улучшения производительности RDP-подключений (не noVNC) включена поддержка UDP на порту 3389.
Если вы подключаетесь через RDP-клиент (например, FreeRDP, mstsc), рекомендуется использовать:
```bash
xfreerdp /v:localhost /u:youruser /p:yourpass /network:lan
```

### 🌐 Доступ

После запуска:
- Веб-доступ через: http://localhost:6080
- Подключение по RDP: localhost:3389
- Терминал через Web: http://localhost:7681

### 📄 Лицензия

Проект распространяется под лицензией MIT. Подробности см. в файле LICENSE.


#### 7. **LICENSE**
Оставьте без изменений, если не меняете лицензию.

---

### ⚠️ Важное уточнение:
Если ваша **основная цель — улучшить стабильность именно noVNC**, то:
- UDP для xrdp **не поможет**, потому что noVNC → VNC → X-server.
- Вместо этого оптимизируйте **VNC-сервер** (например, `x11vnc` с флагами `-noxrecord -noxfixes -noxdamage -tightfilexfer`) или рассмотрите переход на **Guacamole** (поддерживает RDP → WebSocket напрямую).

---

### Альтернатива: Использовать Apache Guacamole
Если вы хотите **RDP + WebSocket + UDP benefit**, лучше использовать **Guacamole**, который:
- Подключается к xrdp по RDP (с UDP),
- Проксирует всё в браузер через WebSocket,
- Полностью поддерживает RDP8/UDP на уровне бэкенда.

Но если вы привязаны к noVNC — тогда UDP в xrdp полезен **только для прямых RDP-клиентов**, а не для веб-доступа.
